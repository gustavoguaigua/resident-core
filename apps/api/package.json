{
  "name": "@resident/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "predev": "pnpm --filter @resident/config build",
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test",
    "prisma:format": "prisma format --schema ../../prisma/schema.prisma",
    "prisma:validate": "prisma validate --schema ../../prisma/schema.prisma",
    "start": "node dist/main.js",
    "pretest": "pnpm --filter @resident/config build",
    "test": "vitest run",
    "pretest:api": "pnpm --filter @resident/config build",
    "test:api": "vitest run test/application-bootstrap.spec.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "11.1.29",
    "@nestjs/config": "4.0.4",
    "@nestjs/core": "11.1.29",
    "@nestjs/platform-express": "11.1.29",
    "@nestjs/throttler": "6.5.0",
    "@resident/config": "workspace:*",
    "class-transformer": "0.5.1",
    "class-validator": "0.15.1",
    "helmet": "8.3.0",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2"
  },
  "devDependencies": {
    "@nestjs/testing": "11.1.29",
    "@prisma/client": "6.19.2",
    "prisma": "6.19.2"
  },
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.21.0"
  }
}
